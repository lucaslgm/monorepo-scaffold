namespace Monorepo.Domain.Interfaces;

/// <summary>
/// Interface marcadora para entidades que devem receber colunas de auditoria
/// (Login, Maquina, DataAtualizacao, etc.) como Shadow Properties.
/// </summary>
public interface IAuditableEntity
{
}
